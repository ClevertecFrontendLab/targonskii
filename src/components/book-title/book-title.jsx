// export const BookTitle = (searchStr, book) => {
//     const paintStr = (string, filter) =>
//     string.replace(new RegExp(`(${filter})`, 'gi'), "<span className='paint-filter'>$1</span>");

//     return (
//         {searchStr ? <p dangerouslySetInnerHTML={{ __html: paintStr(book.title, searchStr) }} /> : <p>{book.title}</p>}
// )}
